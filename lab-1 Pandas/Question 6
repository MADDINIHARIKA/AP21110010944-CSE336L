import pandas as pd
year_month_series = pd.Series(['2023-01', '2023-02', '2023-03', '2023-04'])
specified_day = 15

dates = pd.to_datetime(year_month_series + '-' + str(specified_day), format='%Y-%m-%d')
print("Converted dates:")
print(dates)
